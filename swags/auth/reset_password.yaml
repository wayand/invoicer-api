description: "Reset your password"
parameters:
  - description: "reseting password by using new password and a token"
    in: "body"
    name: "body"
    required: true
    schema:
        type: object
        properties:
            reset_code:
                type: string
            new_password:
                type: string
        example: {"reset_code": "fdgdfg", "new_password": "wewewewe"}


responses:
    200:
        description: a success message
        schema:
            title: Valid Prediction Response
            type: object
            example:
                message: Successfully..
    400:
        description: "Same response is for 202, 400"
